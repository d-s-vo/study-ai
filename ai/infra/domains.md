# Домены проекта

> Актуальные домены, DNS и routing.

Продовые домены/SSL/DNS клиента на момент инициализации проекта не зафиксированы. Таблица ниже
пустая — это валидное состояние на старте: заполнить строки при появлении реальной инфраструктуры
(см. `../infra/README.md` «Статус: не задокументировано»).

## Среды

| Среда | Адрес | Управление | Статус |
|-------|-------|------------|--------|
| Локальный стенд (наш) | см. `../ops/local-setup.md` | `../ops/local-setup.md` | активен |
| Прод | (отсутствует) | (отсутствует) | не зафиксировано |
| Staging | (отсутствует) | (отсутствует) | не зафиксировано |

Legacy-контур (Nuxt, переходный, выводится из эксплуатации): внешний доступ шёл через Cloudflare
Tunnel — без выделенного домена/статичного адреса в текущей документации; относится к старому
контуру, не к Laravel/Sail-стенду.

## Локальная инфраструктура (порты)

См. `../ops/local-setup.md` — единый источник для портов/адресов локального стенда, здесь не
дублируется во избежание рассинхрона.

## SSL / DNS / Routing

Прод-инфраструктура клиента (nginx/edge, DNS-провайдер, кто ведёт SSL — read-only доступ или наша
зона) на момент инициализации проекта не задокументирована — `(отсутствует)`. Заполнить при
появлении реальной прод-инфраструктуры.

## Добавление нового домена

Пошаговая инструкция появится вместе с реальной прод-инфраструктурой (nginx/edge/DNS-провайдер) —
на момент инициализации проекта неприменимо.

1. (уточнить при появлении прод-инфраструктуры)

## Будущие домены

_(пусто)_

<!-- Добавлять строки статусом: 💡 idea / 📋 planned / ✅ running -->
