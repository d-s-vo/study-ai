# ADR-NNN: <пример: фиксация уже действующего решения задним числом>

**Статус:** Принят (зафиксирован задним числом при инициализации `ai/`)
**Дата:** <ГГГГ-ММ-ДД>

> **ОБРАЗЕЦ, не системный ADR.** Это шаблон **decision-ADR для brownfield-сценария**: при развёртывании на
> существующем проекте ты фиксируешь уже действующие архитектурные решения «задним числом», чтобы у базы
> знаний был честный слой «почему». Скопируй под реальным номером `NNN-slug.md` и заполни фактами.
>
> ⚠️ **Не выдумывай обоснование.** Фиксируй только реально действовавшие силы и реально рассмотренные (или
> хотя бы очевидно отвергнутые по факту) варианты. Если чего-то не знаешь — пиши `(история решения не
> восстановлена; зафиксировано как факт текущего состояния)`, а не правдоподобную выдумку. Статус честно
> отражает ретро-фиксацию, а не первичное принятие.

## Контекст

<Реальное ограничение/архитектурная данность, которая УЖЕ действует в коде проекта на момент инициализации.
Пример-заглушка: «Проект использует две БД (основную и справочную) с роутером X. На справочную реально
нужны только таблицы Y; часть исторических data-миграций не учитывает алиас БД и падает под роутером.»
Опиши симптом, который всплыл при локальном развёртывании / инициализации, — именно он делает решение
неочевидным и достойным ADR.>

## Рассмотренные варианты

### Вариант А — <«правильный», но дорогой путь>
- **За:** …
- **Против:** <почему по факту не выбран сейчас: объём легаси, риск для прода, отдельная находка в бэклоге>

### Вариант Б — <то, что реально действует> (принят)
- **За:** <дёшево, воспроизводимо, совпадает с существующим тестовым/рабочим паттерном>
- **Против:** <известные ограничения принятого пути>

## Решение

<Что реально принято как норма проекта — конкретными пунктами. Разведи «как сделано локально/сейчас» и
«правило для нового кода» (напр.: новые миграции обязаны быть `--database`-aware, чтобы не воспроизводить
исходную поломку).>

## Последствия

- <Куда этот инвариант прошит в процессе: напр. пункт в тройной проверке spec (feature-workflow Шаг 5),
  строка в `../guides/stack-specifics.md` §Инварианты.>
- <Что нужно повторять при пересоздании окружения / что остаётся техдолгом.>
- <Если есть открытая находка, которая «правильно» закрыла бы проблему, — сослаться на неё.>

## Ссылки

- <Связанные ADR, spec/impl, dev-доки клиента — ссылками.>
