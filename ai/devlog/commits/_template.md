<!--
ШАБЛОН РАЗБОРА КОММИТА (ADR-009). Копировать в ai/devlog/commits/<repo>/<short-hash>-<slug>.md
Правила заполнения:
  • Два уровня — в ОДНОМ файле (не разносить по файлам): «Кратко» для инженера, «Детально» для новичка.
  • Критерий готовности: по разбору сторонний разработчик восстанавливает «почему сделано именно так»,
    понятно ДАЖЕ новичку.
  • НЕ дублировать содержимое impl.md/spec/ADR — на них ССЫЛАТЬСЯ (секция «Связи»).
  • patch-id: `git show <hash> | git patch-id --stable` (из клиентского worktree).
  • short-hash — as-authored (на feat/<slug> в момент коммита), НЕ финальный клиентский хеш.
  • После создания — добавить строку в ai/devlog/commits/README.md (append-only) и коммит через ai-commit.sh.
  • Удалить эти комментарии-подсказки перед сохранением необязательно, но желательно.
-->
---
repo: <repo-slug>             # одно из cbook
authored_hash: <full-sha>     # SHA на feat/<slug> в момент коммита (стабильный ключ)
patch_id: <patch-id>          # git patch-id --stable (переживает rebase)
branch: feat/<slug>
feat: FEAT-NNN
date: YYYY-MM-DD
final_hash:                   # клиентский SHA после squash/rebase MR — best-effort, можно позже
---

# <repo>@<short-hash> — <заголовок коммита>

> Коммит-сообщение (как в клиентском репо): `<type>: <описание> (conventional commits; type ∈ feat|fix|chore|refactor|docs|test|style|perf|build|ci)`

## Кратко (для инженера)

<!-- По каждому изменённому файлу 1-3 строки: ЧТО изменилось и ПОЧЕМУ. Термины без расшифровки. -->

- `path/to/file.ext` — что/почему.
- `path/to/other.ext` — что/почему.

## Детально (для новичка)

<!-- Каждое изменение простым языком. Расшифровывай термины и приёмы ПО МЕСТУ (что такое миграция,
     зачем констрейнт, почему выбран этот паттерн и т.п.). Для каждого: почему выбран именно этот
     приём; где лежит файл и почему именно там. -->

### `path/to/file.ext`
Что делает изменение простыми словами. Что за термины здесь встречаются и что они значат.
Почему выбран этот приём (а не более простой/очевидный). Почему файл лежит именно здесь.

### `path/to/other.ext`
…

## Почему так, а не иначе

<!-- Отвергнутые альтернативы ДЛЯ РЕШЕНИЙ ЭТОГО КОММИТА: что рассматривали, почему не взяли. -->

- Альтернатива X — отклонена, потому что …
- Альтернатива Y — отклонена, потому что …

## Связи

<!-- Только ссылки, без пересказа содержимого. -->

- Фича: `../features/FEAT-NNN-slug/impl.md` · spec (подставь реальный FEAT-slug).
- ADR/keystone: `../../adr/NNN-slug.md` (если коммит затрагивает архитектурное решение).
- Смежные коммиты фичи: `<repo>/<hash>-<slug>.md`.
