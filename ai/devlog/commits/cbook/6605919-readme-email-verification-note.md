---
repo: cbook
authored_hash: 6605919034728fc55a520cda782848a102c160ef
patch_id: 49d4f3e30e8c403a8a1b2b3b6c4f0ac26882332e
branch: feat/auth-hardening
feat: FEAT-007
date: 2026-07-31
final_hash:
---

# cbook@6605919 — docs: отметить обязательное подтверждение email в инструкции запуска

> Коммит-сообщение (как в клиентском репо): `docs: отметить обязательное подтверждение email в инструкции запуска`
> Doc-only звено цепочки `a1f6c53` → `f4a9d63` (FEAT-007). 1 файл, +3 строки.

## Кратко (для инженера)

- **`README.md`** — в секции «Локальный запуск (Sail)», сразу после строки про порты (`APP_PORT`/Vite/Filament),
  добавлен абзац: регистрация требует подтверждения email (до перехода по ссылке из письма доступна только
  страница с просьбой подтвердить адрес); локально письма перехватывает Mailpit (UI на `:8025`).

## Детально (для новичка)

Правило проекта: user-visible смена поведения отражается в публичной доке клиента. `f4a9d63` включил
обязательную верификацию email — новый разработчик, подняв стенд и зарегистрировавшись, упирается в
notice-страницу вместо dashboard и без подсказки пойдёт искать «баг». Три строки в README снимают вопрос
и говорят, где искать письмо (Mailpit уже упомянут в этой же секции как часть окружения `sail up`).

## Почему так, а не иначе

- Место — секция локального запуска, а не «Архитектура»: это операционный факт для того, кто поднимает
  проект, рядом с существующим упоминанием Mailpit.
- Отдельный `docs:`-коммит, а не довесок к `f4a9d63` — по согласованию: код и документация разнесены,
  diff каждого звена остаётся одноцелевым.
- Формулировка нейтральная, продуктовая — без деталей реализации и внутренних процессов.

## Гейты

Doc-only: код/тесты/конфиги не затронуты — полный прогон не требовался; состояние гейтов цепочки
зафиксировано в `f4a9d63-auth-hardening.md` (Pest 39, PHPStan L10 без baseline, Pint, живая приёмка).

## Связи

- Головной разбор цепочки: `a1f6c53-auth-hardening.md` (fix_chain); поведение — `f4a9d63-auth-hardening.md`.
- Ревью: `6605919-review.md`. Фича: `../../features/FEAT-007-auth-hardening/impl.md`.
