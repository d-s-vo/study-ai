# 2026-07-31

## Решения

- Владелец: доменная модель дополнена `recipes.user_id` + `RecipePolicy` (в ТЗ поля не было; без владения защита от IDOR невозможна).
- Владелец: модель просмотра — **общий каталог** (index/show всем auth+verified; мутации — владелец).
- Владелец: `/profile` переводится под `verified` (nit ревью FEAT-007) — включено в спеку FEAT-010.
- Оркестратор: пакет домена нарезан на FEAT-009 (data-layer) / FEAT-010 (crud) / FEAT-011 (pages), specs-first.

## Quickfixes

- quickfix-coord-feat-validation: `scripts/coord.sh book` — кросс-проверка реестров FEAT против факта
  (папок `devlog/features/FEAT-NNN-*`): расхождение → громкий отказ с рецептом починки через
  `feat-map.sh add` (1 файл, ~30 LOC). Мотив — инцидент выдачи занятых номеров 2026-07-31
  (закрытие FEAT-005…008 мимо Шага 10); red→green доказан симуляцией сталого реестра (FEAT-099-папка
  → отказ; уборка → штатная выдача следующего номера).
