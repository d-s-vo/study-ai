# stopwords.txt — стоп-словарь охранной обвязки клиентских реп (МОДУЛЬ M6).
# Одна regex (grep -E, БЕЗ -P) на строку. Пустые строки и '#'-комментарии игнорируются.
# Скан кейс-инсенситивный (grep -i). Матч ЛЮБОЙ строки => блокировка.
# Ложные срабатывания гасятся через allowpatterns.txt.
#
# СТРУКТУРА: переносимое ЯДРО следов ИИ/вендоров (оставить как есть) + {{...}}-слоты под
# конкретный проект (имя внутреннего репо, внутренние хосты/IP, свои термины системы).
# ⚠ Заглушки {{...}} обязаны быть ЗАМЕНЕНЫ (иначе матчатся как литералы и/или бесполезны).
#
# --- переносимое ядро: следы ИИ/агентов/вендоров ---
claude
anthropic
anthropic\.com
\bcodex\b
copilot
chatgpt
gpt-[0-9]
\bllm\b
\bopenai\b
orchestrator
sub-?agent
\bklod\b
\bkloud\b
нейросет
Нейросет
# 'ассистент' блокируем ТОЛЬКО в связке с AI/ИИ (голый 'ассистент' — легитимный термин).
(ai|ии)[- ]*ассистент
ассистент[- ]*(ai|ии)
(ai|ии)[- ]*Ассистент
Ассистент[- ]*(ai|ии)
#
# --- переносимое ядро: следы системы ai/ и процесса (шаблон использует эти же имена) ---
ai[-_/]context
devlog
spec\.md
impl\.md
retro\.md
оркестратор
Оркестратор
субагент
Субагент
спека
Спека
#
# --- переносимое ядро: маркеры задач/номера фич ---
\bFEAT-[0-9]
# feat_001 / feat123 (варианты без дефиса). \b перед feat отсекает 'defeat2' и подобные.
\bfeat[_]?[0-9]
# '#NNN' блокируем ТОЛЬКО рядом со словом feature/фича (голый '#123' в коде легитимен).
[#][0-9]+[[:space:]]*(feature|фича|Фича)
(feature|фича|Фича)[[:space:]]*[#][0-9]+
~[[:space:]]*wip[[:space:]]*~
#
# --- переносимое ядро: трейлеры авторства ИИ ---
co-authored-by
generated with
#
# --- ПРОЕКТНЫЕ СЛОТЫ (замените под свой проект; по одному regex на строку) ---
# Имя внутреннего репо артефактов (внутренняя зона знаний проекта):
study-cbook-ai
# Внутренние хосты/термины/IP (git-хост артефактов, VPN-адрес, внутренние домены) —
# добавьте по одному regex на строку, экранируя точки (напр. 10\.0\.0\.1 или git\.internal\.example):
# 
# 
