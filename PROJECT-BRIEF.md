# PROJECT-BRIEF — контекст для агента, пишущего задачи (промпты)

Прочитай это ПЕРЕД тем, как формулировать любую задачу разработчику/исполнителю.
Цель брифа — чтобы план разработки был скорректирован под реальную ситуацию, а не под
ошибочную рамку «миграция Nuxt → Laravel 1:1».

---

## 0. Двухконтурная система разработки — ГДЕ и КАК исполняется задача (читай ПЕРВЫМ)

Разработка идёт в **двухконтурной модели**. Не понимать её = ставить задачи, которые исполнитель
выполнит не в том каталоге или которые заблокирует охранная обвязка.

### Два контура (не путать)
- **Workspace / репозиторий знаний** (`study-cbook-ai`): содержит `ai/` (архитектура, гайды, ADR,
  память), `scripts/` (интерфейс), `githooks/` (охрана), а также `CLAUDE.md` (это устав **самой
  агентской системы**, НЕ проекта!), `PROJECT-BRIEF.md`, `DEPLOY.md`. Здесь живёт весь контекст и
  методология. **Клиентского кода тут нет.**
- **Клиентский репозиторий** (`nuxt4-ts-project-cbook`): реальный код проекта (сейчас legacy Nuxt,
  переписывается на Laravel). Физически лежит ВНУТРИ workspace как gitignored bare-клон + worktree.

### Где исполняется ЛЮБАЯ твоя задача
Исполняющий агент заводит worktree задачи `tasks/<slug>/` (чекаут **клиентского** репо) и работает
**только там**.
- **«Корень проекта» в любой задаче = корень этого worktree** (клиентский проект), а НЕ корень workspace.
- В зоне работы (`tasks/<slug>/`) **НЕТ и не может быть** `ai/`, `scripts/`, `githooks/`, системного
  `CLAUDE.md`, `PROJECT-BRIEF.md`, `DEPLOY.md` — они в другом контуре (workspace).
- Поэтому **НЕ пиши в задачах «не трогай `ai/`/`PROJECT-BRIEF.md`»** — их там нет; это создаёт ложное
  впечатление, что всё в одной папке. Вместо этого явно фиксируй: *«все действия — в корне клиентского
  проекта (твой task-worktree), это НЕ корень workspace»*.
- `.git` в worktree — это **файл-гитлинк** (общий с bare), а не папка. `git stash` там запрещён.

### Как задача попадает в код и наружу (постановщику ЗНАТЬ, в промпт НЕ включать)
Исполнитель сам оборачивает задачу в цикл workspace: `task.sh new <slug>` → работа в `tasks/<slug>` →
`commit.sh` → **человек** `deliver.sh` → PR `feat → develop → main`.
> В промпт **НЕ включай**: `git push`, PR, deploy, `task.sh`, выбор/создание веток. Это делает
> исполнитель/человек. **Промпт = чистая спека «что построить + DoD».**

### Режим «0 следов ИИ» — обвязка реально блокирует (учитывай при постановке)
`commit.sh` и git-хуки сканируют **дифф И ИМЕНА файлов** на стоп-слова (`claude`, `anthropic`, `ai`,
`gpt`, `orchestrator`, `subagent`, `devlog`, `spec.md`, `impl.md`, `~wip~`, `co-authored-by`, имя
внутреннего репо и т.п.). Практика для задач:
- НЕ создавать `CLAUDE.md`, `AGENT*.md` (имя `CLAUDE.md` само = стоп-слово, блокируется даже на
  удаление); не упоминать ИИ/агентов в коде, комментариях, сообщениях коммитов.
- Осторожно с именами файлов в клиентском репо: избегать `*spec.md`, `impl.md`, `retro.md`, `*.jsonl`,
  чего-либо с `devlog`.
- Секреты/`.env` не коммитить. Сообщения коммитов — conventional (`feat|fix|chore|…: описание`).
- Источник истины архитектуры (`ai/…`) исполнитель читает из workspace; **но в клиентской задаче
  НЕ инструктируй «создать/править `ai/`»** — это другой контур (свой канал `ai-commit.sh`).

---

## 1. Настоящая цель — ОБУЧЕНИЕ, а не продукт

Владелец учится **собирать и сопровождать enterprise-монолиты на Laravel** в стиле своих
референс-проектов **`listvafintess`** и **`av1`** (его существующие монолиты — эталон паттернов,
которые он хочет освоить). Их конкретные конвенции в этом workspace НЕ задокументированы —
**если нужны детали их структуры, спроси у владельца**, не выдумывай.

Учебный проект **Cookbook** — это **подопытный кролик** для отработки паттернов, а НЕ продукт,
который надо сохранить или точно перенести.

> **Следствие для планирования:** задачи должны быть **педагогичными** — инкрементальными,
> с пояснением «зачем» каждого enterprise-паттерна (Task / Repository / Resolver / DTO), с
> обозримыми диффами, которые владелец может отсмотреть и понять. Никаких «чёрных ящиков»
> и мега-задач на пол-проекта.

## 2. Это ПЕРЕПИСЫВАНИЕ, а не миграция

Стек Nuxt/Prisma **списан** — он больше не актуален. Мы **не портируем** Nuxt-код 1:1.

Сохраняем только:
- **общую идею** (кулинарная книга: список / просмотр / добавление с картинкой / удаление рецептов);
- **специфичные переиспользуемые блоки**, в первую очередь **BVI-панель** (доступность для
  слабовидящих: размер шрифта, темы, синтез речи — клиентская логика, переносится как
  Inertia/Vue-компонент, от бэкенда не зависит).

Всё остальное **строится заново** на целевом стеке. Известные дыры Nuxt-версии (нет auth, нет
серверной валидации, утечка ошибок наружу, небезопасная загрузка файлов) — это то, что нужно
**исправить/не воспроизводить**, а не переносить. Nuxt-код = **референс идеи и BVI**, не образец
для копирования.

## 3. Целевой стек и архитектура (источник истины — `ai/`, НЕ `CLAUDE.md`)

⚠️ Клиентский `CLAUDE.md` **удалён**. Правила проекта живут в workspace-каталоге `ai/`:
- `ai/architecture.md` (§0 роутер, §4 слои, §5 домен),
- `ai/guides/stack-specifics.md` (команды/тулчейн/инварианты),
- `ai/adr/002-*` (стек), `ai/adr/003-*` (слои), `ai/memory.md` (north star + статус).
Ссылайся в промптах на них.

Стек: **PHP 8.4 · Laravel 12 · MySQL 8 · Sail (Docker)**; Inertia + Vue 3 (`<script setup>`, TS strict)
+ Tailwind; **Spatie Laravel Data** (DTO) + `artisan typescript:transform`; **Filament 5**;
`whyme-agency/laravel-media` (медиа); качество — **Larastan (PHPStan level 10)** + **Pint** + **Pest**.

Строгий поток: `Request (FormRequest) → Controller → Task → Repository → DTO (Spatie) → Page Resolver → Inertia (Vue)`.
Незыблемо: `declare(strict_types=1)` в каждом `.php`; Eloquent/`DB` — только в `app/Data/Repositories/*`;
наружу (в Inertia/Vue) — **только DTO**, не сырые модели; вся серверная валидация — через Form Requests.

## 4. Реальное состояние репозитория (критично для плана; срез 2026-07-31, develop@`18c7282`)

**Фундамент построен и «вылечен» — legacy Nuxt из репо полностью удалён** (референс лежит в
workspace `local/legacy_nuxt/`, вне клиентского репо). В `develop`/`main` влиты FEAT-002…008:

- **Скелет:** Laravel **12.64** (pinned) / PHP 8.4 / Sail (MySQL 8 `:3306` + Redis + Mailpit `:8025`);
  `app/` — бэкенд Laravel, фронт — Inertia + Vue 3 TS strict в `resources/js` (Breeze-скелет),
  Tailwind **v3**, Node-менеджер — **pnpm**.
- **Слои:** `app/Tasks/` (канон: пустой `BaseTask`-ярлык; конкретный Task — `final`, DI в
  конструкторе `private readonly`, данные — аргументы метода **`run()`**, НЕ `handle()`),
  `app/Data/Repositories/` (единственное место Eloquent/`DB`), `app/Data/` (DTO Spatie),
  `app/Resolvers/Page/` (пока пуст). Изоляция Eloquent **энфорсится Pest arch-тестом**
  (`tests/Feature/ArchitectureTest.php`, red→green доказан).
- **Auth (рабочий, закалённый):** Breeze Inertia + рефактор в слои (`UserRepository`,
  `RegisterUserTask`); **email-верификация ВКЛЮЧЕНА** (`User implements MustVerifyEmail`,
  dashboard под `verified`); throttle на register/forgot/reset; вся валидация — FormRequest;
  в Inertia уходит **`UserData` DTO** (не сырая модель), TS-типы автогенерятся
  (`artisan typescript:transform` → `resources/js/types/generated.d.ts`, коммитится).
- **Качество:** PHPStan **L10 без baseline (0 подавлений)**, Pint (`pint.json`, psr12+strict),
  Pest (43 passed), ESLint flat-config + `pnpm lint`/`typecheck`, **CI GitHub Actions**
  (`.github/workflows/ci.yml`: Pint/PHPStan/Pest на MySQL-сервисе/lint/typecheck/build).
  `.env.testing.example` — шаблон тестового окружения (живой `.env.testing` gitignored).
- **Домена ещё НЕТ:** `Recipe`/`Ingredient`, Filament 5, `whyme-agency/laravel-media`, BVI —
  не начаты. Filament и media **ещё не установлены** в composer.

## 5. План дальнейшей разработки

1. Рамка прежняя: **«переписывание с нуля, переиспользуя идею + BVI»**, а не миграция.
2. Сделано ✅: скелет (1a, FEAT-002), enterprise-барьеры (1b, FEAT-003), auth+фронт-скелет
   (FEAT-004), инфра/CI + DTO-граница + auth-hardening + фронт-гигиена (FEAT-005…008).
3. Следующее (порядок):
   - **2+ — Доменные срезы снизу-вверх:** миграция → Eloquent-модель → Repository → DTO → Task →
     FormRequest → Controller → Resolver → Inertia-страница → Pest; сущности `Recipe`/`Ingredient`
     (домен в `ai/memory.md` §North Star: steps — JSON-массив, ingredients — hasMany).
     Высокорисковые зоны: Policies/владение (IDOR), N+1 на листингах, пагинация.
   - **Filament 5** (установка = генератор: сразу ревиз-проход «генерённое vs STRICT»).
   - **`whyme-agency/laravel-media`** (загрузка изображений — безопасно, не как в Nuxt).
   - **BVI-панель** как Inertia/Vue-компонент (донор — `local/legacy_nuxt/`).
4. Инструменты гонять **через Sail** (`sail bin pint`, `sail bin phpstan analyse`, `sail artisan test`);
   новые правила/гейты — **доказывать red→green**; подавления (baseline и т.п.) запрещены без
   номера фичи-погашения. Ревью каждого коммита обязательно (ADR-010), рубрика — 
   `ai/guides/commit-review.md` (вкл. п.8 «чужая среда»: CI-раннер, `--no-dev`-прод, чистая машина).

## 6. Ограничения процесса (чтобы промпты ложились в наш пайплайн)

- Любая правка клиентского репо идёт **циклом workspace**: `task.sh new <slug>` → работа в
  `tasks/<slug>` → `commit.sh` (скан чистоты + conventional) → пользователь `deliver.sh` →
  PR `feat → develop → main`. Промпт — это **спека задачи**; исполнитель сам оборачивает её в цикл.
  **НЕ включай в промпты шаги push/deploy** — они только человеку.
- **Режим «0 следов ИИ»** в клиентском коде/коммитах: не добавлять `CLAUDE.md`/`AGENT*.md`,
  не упоминать ИИ/агентов в коде и сообщениях коммитов.

## 7. Открытые решения — уточнить у владельца

- Диск для `whyme-agency/laravel-media`: локальный или S3/MinIO.
- Какие конкретно паттерны/конвенции из `listvafintess` и `av1` воспроизводить (если владелец
  может поделиться их структурой — это лучший образец для «enterprise-стиля»; частично уже
  выверено: канон Task `run()`/`final readonly` снят с `listvafitness`, см. `ai/memory.md` §Gotchas).
- Нужен ли `verified`-middleware на `/profile` (сейчас auth-only — дефолт Breeze; nit из ревью FEAT-007).

Решено ранее (НЕ переспрашивать): стратегия `app/` — Nuxt замещён целиком (сделано);
СУБД — MySQL 8; email-верификация — включена (решение владельца 2026-07-31);
CI — GitHub Actions в клиентском репо (работает).

---

**Короткая суть для автора промптов:** это учебный **greenfield-Laravel-монолит** для освоения
enterprise-паттернов; Nuxt-код — донор идеи и BVI (лежит в workspace `local/legacy_nuxt/`, в клиентском
репо его больше нет). **Фундамент готов**: скелет Laravel 12 + слои (канон Task = `run()`, `final readonly`)
+ рабочий auth с email-верификацией + DTO-граница + QA-тулчейн (PHPStan L10 без подавлений, arch-тесты,
ESLint, CI). **Следующий фронт — домен `Recipe`/`Ingredient`** (срезы снизу-вверх), затем Filament 5,
media, BVI. Правила — в `ai/`; задачи — инкрементальные и объясняющие «почему»; каждый коммит проходит
независимое ревью.